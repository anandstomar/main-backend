class DiseasePredictionModel {
    constructor(modelID, modelName, version, trainingDataSize) {
        this.modelID = modelID;
        this.modelName = modelName;
        this.version = version;
        this.trainingDataSize = trainingDataSize;
    }
}
